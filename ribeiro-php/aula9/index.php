<?php
    // $a = 100;
    // $b = 200;

    // $x = ($a < $b) && ($a < 1000);
    // $x = ($a > $b) || ($a > 1000);
    // $x = ($a < 150) && ($b > 300);
    // $x = ($a > 10) || ($b > 500);
    // $x = ($a == 100) && ($b < $a);

    // var_dump($x);

    ///////////////////////////////////////////////////////////////////////////

    // $x = (10 > 5) && (20 > 30);
    // $x = (2 > 1) && (3 > 2);
    // $x = (2 < 4) || (4 > 10);
    // $x - (2 == 3) || (3 > 1);


    ///////////////////////////////////////////////////////////////////////////

    $a = false;

    $x = !$a;

    var_dump($x);
?>